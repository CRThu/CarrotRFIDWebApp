[project]
name = "carrotrfidwebapp"
version = "0.1.0"
description = "Carrot RFID Web App"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "carrotrfidtester",
    "fastapi",
    "loguru",
    "pyserial",
    "uvicorn[standard]",
]

[tool.uv.sources]
carrotrfidtester = { workspace = true }
