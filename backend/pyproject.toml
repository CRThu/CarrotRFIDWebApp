[project]
name = "carrotrfidwebapp"
version = "0.1.1"
description = "Carrot RFID Web App"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nfctester",
    "fastapi",
    "loguru",
    "pyserial",
    "uvicorn[standard]",
]

[tool.uv.sources]
nfctester = { workspace = true }
